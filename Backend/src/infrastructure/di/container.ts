/**
 * DI Container — canonical location as per Back.md specification.
 *
 * Re-exported from shared/container.ts for backwards compatibility.
 * This file is the "source of truth" location in the architecture.
 */
export { createContainer } from '../../shared/container';
export type { AppContainer } from '../../shared/container';
